version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - tsts-network

networks:
  tsts-network:
    external: true
